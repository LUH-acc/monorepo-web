# 三项目状态看板

| 项目 | 目录 | 技术栈 | 状态 | 默认端口 |
|------|------|--------|------|----------|
| A | `apps/project-a` | Node（可按原栈迁入） | 已上线，待迁入代码 | 3001 |
| B | `apps/project-b` | Vite + Vue 3 + TS | 脚手架就绪 | 3002 |
| C | `apps/project-c` | Vite + Vue 3 + TS | 脚手架就绪 | 3003 |

## 本周建议

1. 完成 A 代码迁入与 `pnpm dev:a` 验证。
2. B、C 在 `PLAN.md`（可自行添加）中写清 MVP，再开工。
3. 新功能开发前先判断：是否应放入 `packages/core`。
